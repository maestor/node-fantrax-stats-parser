interface:
  display_name: "Git PR Workflow"
  short_description: "Run consistent PR handoffs and safe post-merge cleanup"
  default_prompt: "Use $git-pr-workflow to clean this workspace after a merged PR: refresh main and remove only the active local branch, trusting my merge confirmation."
