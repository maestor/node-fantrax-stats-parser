interface:
  display_name: "Git PR Workflow"
  short_description: "Run consistent PR handoffs and safe post-merge cleanup"
  default_prompt: "Use $git-pr-workflow to clean this workspace after merged PRs: update main and safely remove local branches already merged into it."
